<%= form_with model: [@user, @product, @comment], local: true do |form| %>

<%= form.label :text %>
<%= form.text_field :text %>

<%= form.hidden_field :author_id, value: @user.id %>
<%= form.hidden_field :product_id, value: @product.id %>

 <%= form.submit %>

<% end %>