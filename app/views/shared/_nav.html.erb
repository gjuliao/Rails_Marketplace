<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <%= link_to 'Marketplace', root_path, class: 'navbar-brand' %>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                        <%= link_to 'Home', root_path, class: 'nav-link' %>
                        <li class="nav-item"><a class="nav-link" href="#!">About</a></li>
                        <li class="nav-item"><a class="nav-link" href="#!">Contact</a></li>
                        <% if user_signed_in? %>
                            <li class="nav-item dropdown">
                                <%= link_to 'My Profile', root_path, class: 'nav-link dropdown-toggle', role: "button", data: { bs_toggle: 'dropdown', aria_expanded: 'false' } %>
                                <ul class="dropdown-menu">
                                    <li class="dropdown-item">
                                        <%= link_to 'Add a product', new_user_product_path(current_user), class: 'dropdown-item' %>
                                    </li>
                                    <li class="dropdown-item">
                                        <%= link_to 'My Products', my_products_user_path(current_user), class: 'dropdown-item' %>
                                    </li>
                                    <li class="dropdown-item">
                                        <%= link_to 'My Events', my_events_user_path(current_user), class: 'dropdown-item' %>
                                    </li>
                                    <li class="dropdown-item">
                                        <%= link_to 'Edit Profile', edit_user_registration_path, class: 'dropdown-item' %>
                                    </li>
                                    <li class="dropdown-item">
                                        <%= link_to 'Log Out', destroy_user_session_path, class: 'dropdown-item', method: :delete %>
                                    </li>
                                </ul>
                            </li>
                        <% else %>
                            <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %>
                        <% end%>
                    </ul>
                </div>
            </div>
</nav>